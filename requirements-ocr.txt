# OCR / extracción PDF avanzada (local o workers con binarios de sistema).
# En Streamlit Cloud free suele faltar poppler/tesseract → el pipeline degrada a nativo.
#
# Sistema (ejemplos):
#   Windows:  choco install poppler tesseract
#   Debian:   apt-get install -y poppler-utils tesseract-ocr tesseract-ocr-spa
#
pymupdf>=1.24.0
pdf2image>=1.17.0
pytesseract>=0.3.10
Pillow>=10.4.0
